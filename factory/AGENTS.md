# The Factory — repository instructions

`CLAUDE.md` is a symlink of this file; break the link only if tool-specific instructions must diverge.

For maintenance and design sessions in this directory:

- **Orient:** `README.md`, then `design/INDEX.md` — the governing map of the published design set.
- **Before creating, moving, superseding, or renaming any document:** read `design/DOC-PROTOCOL.md` — the canonical schema and write protocol.
- **Code or config changes:** run the touched modules' tests.

Harness-spawned seats never load this file (workspaces live outside the repo, at `~/l1-l5-workspaces`). If you are reading this as a spawned seat anyway, your composed identity prompt and node brief govern — not this file.
